from setuptools import setup

VERSION = "0.1"

setup(name="pymultileveldb",
      version=VERSION,
      author="Will Moss",
      author_email="wbmoss@gmail.com",
      packages=["pymultileveldb"],
      install_requires=[],
)
